# cp .git.shared.config .git/shared.config
# git config set --local include.path shared.config

[commit]
  template = .git.commit.template

[alias]
  shared-config-diff = diff --no-index -- .git.shared.config .git/shared.config
  shared-config-cp = !cp .git.shared.config .git/shared.config
